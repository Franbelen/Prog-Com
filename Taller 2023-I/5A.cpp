#include <bits/stdc++.h>
using namespace std;

vector<int> V, N;
int S, b, Times;
int M =10;

vector<int> recursion(vector<int> arr, int i) {
    if (i == M){
        return arr;
    }
}

int main()
{
    cin >> Times;

    # repetición
    N.clear(), V.clear();
    for (int j = 10; j>0 , j--)
        N.push_back(j);
    
    for (int i = 0; i < M; i++)
    {
        cin >> b;
        V.push_back(b);
    }

}